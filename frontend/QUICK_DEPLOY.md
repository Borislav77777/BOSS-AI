# 🚀 Быстрый деплой BARSUKOV OS

## ⚡ Одна команда для деплоя

```bash
npm run deploy:safe
```

## 🎯 Что делает эта команда:

1. ✅ **Создает production сборку** - `npm run build:prod`
2. ✅ **Создает архив** - `deploy-archive.tar.gz`
3. ✅ **Создает backup на сервере** - автоматически
4. ✅ **Очищает старые файлы** - удаляет устаревшие JS/CSS
5. ✅ **Загружает новые файлы** - распаковывает архив
6. ✅ **Проверяет работу сайта** - HTTP 200 check
7. ✅ **Откатывает при ошибке** - восстанавливает backup

## 📋 Требования:

- SSH доступ к серверу (уже настроен)
- Git Bash / WSL / Linux terminal

## 🔧 Использование:

### Обычный деплой
```bash
npm run deploy:safe
```

### Если нужно только собрать (без загрузки)
```bash
npm run build:prod
```

### Если нужно проанализировать размер бандла
```bash
npm run analyze
```

## 📊 Что происходит на сервере:

### Backup'ы
```
/home/admin/web/ballu-splitsistema.ru/backups/
├── backup_20250110_143022.tar.gz
├── backup_20250110_152341.tar.gz
└── backup_20250110_163512.tar.gz
```

Автоматически хранятся **5 последних backup'ов**.

### Структура сайта
```
/home/admin/web/ballu-splitsistema.ru/public_html/
├── index.html
├── assets/
│   ├── index-f3044e69.js
│   ├── index-e128507a.css
│   └── ...
├── services/
└── logo.svg
```

## 🆘 Откат на предыдущую версию

### Автоматический откат
Если сайт не отвечает HTTP 200, скрипт предложит откатить изменения.

### Ручной откат
```bash
ssh admin@217.12.38.90

cd /home/admin/web/ballu-splitsistema.ru
ls -lt backups/  # Смотрим доступные backup'ы

# Выбираем нужный backup
tar -xzf backups/backup_20250110_143022.tar.gz
```

## ⚙️ Переменные окружения

Скрипт использует:
- **IP сервера:** 217.12.38.90
- **Пользователь:** admin
- **Домен:** ballu-splitsistema.ru
- **Путь:** /home/admin/web/ballu-splitsistema.ru

Изменить можно в файле `deploy-safe.sh`

## 🎯 Workflow разработки:

```bash
# 1. Разрабатываем локально
npm run dev

# 2. Тестируем
npm run test

# 3. Проверяем типы
npm run type-check

# 4. Деплоим
npm run deploy:safe

# 5. Проверяем сайт
# http://ballu-splitsistema.ru
```

## 📈 Лучшие практики:

1. **Тестируйте локально** перед деплоем
2. **Коммитьте изменения** в Git перед деплоем
3. **Проверяйте сайт** после деплоя
4. **Следите за размером бандла** - `npm run analyze`
5. **Не удаляйте backup'ы** вручную

## 🔐 Безопасность:

- ✅ Backup создается **перед** каждым деплоем
- ✅ Хранятся **5 последних** версий
- ✅ Проверка работы сайта **после** деплоя
- ✅ Автоматический откат при **ошибке**
- ✅ Сохранение `.htaccess` файла

## 🎉 Готово!

Теперь вы можете деплоить одной командой:
```bash
npm run deploy:safe
```

---

**Примечание:** При первом запуске может потребоваться ввод пароля SSH. Для автоматизации настройте SSH ключи.
