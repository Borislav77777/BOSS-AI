# 🎯 Правила работы с платформой BARSUKOV OS

## 🔍 ОСНОВНЫЕ ПРАВИЛА
- ЭТО ОЧЕНЬ ВАЖНО Не нарушать структуру проекта, смотреть структуру в файле README.md , обновлять структуру в этом же файле, если требуется что то добавить! Если этого файла нет в проекте то создавать его и описывать там структуру !
- ЭТО ОЧЕНЬ ВАЖНО ВСЕГДА ДЕРЖАТЬ В УМЕ КОНЕЧНУЮ ЦЕЛЬ ПРОГРАММЫ И ДЕЙСТВОВАТЬ ИСХОДЯ ИЗ ЦЕЛИ ВСЕЙ ПРОГРАММЫ, УЧИТЫВАТЬ ДЛЯ ЧЕГО ПРОГРАММА И ЧТО ОНА ДОЛЖНА ДЕЛАТЬ В ИТОГЕ !
- ЭТО ОЧЕНЬ ВАЖНО ВСЕГДА делать систему максимально модульной ! в процессе разработки и добавления новых функций программы !!!  изменения записывать в README.md если структуруа меняется ( расширяется )
- ЭТО ОЧЕНЬ ВАЖНО ЕСЛИ ТЫ ВИДИШЬ В ПРОЦЕССЕ РАЗРАБОТКИ ЧТО ФАЙЛЫ ПРОЕКТА СТАНОВЯТСЯ СЛИШКОМ БОЛЬШИМИ И С НИМИ ТЯЖЕЛО РАБОТАТЬ СТАРАЙСЯ РАЗБИТЬ ИХ НА МОДУЛИ ЕСЛИ ЭТО ВОЗМОНЖО И ОБНОВЛЯЙ README.md
- ЭТО ОЧЕНЬ ВАЖНО ВСЕГДА ПИШИ ДОК СТРИНГИ ДЛЯ СЕБЯ, ЧТОБ ТЫ НЕ ЗАПУТАЛСЯ!
- Сначало всегда делать исследование по посталвленной задаче, и смотреть варанты решения в интернет
- После исследования проблемы, исследовать очень глубоко структур самой программы, смотреть все её логики
- После глубоких исследований писать себе тз для исправления и потом только вносить правки, по своему тз
- НЕ СОЗДАВАТЬ БЕСПОЛЕЗНУЮ ДОКУМЕНТАЦИЮ: не писать новые .md файлы после каждого промпта, не создавать отчеты которые ничего не объясняют, ВМЕСТО ЭТОГО - если нужно документировать изменения в логике программы, то ОБНОВЛЯТЬ существующие файлы проекта (README, комментарии в коде), файлы тестов удалять после успешного тестирования
- Вносить правки МАКСИМАЛЬНО ОСТОРОЖНО: НИКОГДА не ломать работающие функции, всегда тестировать каждое изменение отдельно, проверять что ВСЕ связанные части системы продолжают работать после правки, делать изменения по одному за раз и сразу проверять результат, ОБЯЗАТЕЛЬНО ДОКАПЫВАТЬСЯ ДО КОРНЯ ПРОБЛЕМЫ - минимум 3-5 источников информации, изучить все связанные файлы, понять ВСЮ цепочку зависимостей, найти ВСЕ места где может проявиться проблема, исследовать до тех пор пока не будешь на 100% уверен в понимании

## 🎛️ КРИТИЧЕСКИЕ ПРАВИЛА РАБОТЫ С ВИДЖЕТАМИ

### ⚡ ПРИНЦИПЫ РАБОТЫ С ВИДЖЕТАМИ - ЗАПОМНИ НАВСЕГДА!

#### ✅ ЧТО МОЖНО И НУЖНО ДЕЛАТЬ С ВИДЖЕТАМИ:
- **ЧИНИТЬ** - исправлять ошибки в коде виджетов (синтаксис, логика, совместимость)
- **УЛУЧШАТЬ** - добавлять новый функционал, оптимизировать производительность
- **ОЧИЩАТЬ** - удалять дублированные файлы, бэкапы, временные файлы виджетов
- **ОБНОВЛЯТЬ** - приводить к совместимости с новыми версиями PHP/OpenCart
- **ТЕСТИРОВАТЬ** - проверять работоспособность после каждого изменения

#### ❌ ЧТО КАТЕГОРИЧЕСКИ ЗАПРЕЩЕНО:
- **НЕ УДАЛЯТЬ** - сами виджеты и их основной функционал
- **НЕ ОТКЛЮЧАТЬ** - рабочие виджеты без крайней необходимости
- **НЕ ЛОМАТЬ** - существующую логику работы виджетов
- **НЕ МЕНЯТЬ** - основную архитектуру виджета без понимания последствий

#### 🔧 ОБЯЗАТЕЛЬНЫЙ АЛГОРИТМ РАБОТЫ С ВИДЖЕТАМИ:
1. **ПЕРЕД ИЗМЕНЕНИЕМ**: всегда делать бэкап виджета
2. **ИССЛЕДОВАНИЕ**: глубоко изучить код и логику виджета
3. **ПЛАНИРОВАНИЕ**: составить план изменений с пониманием последствий
4. **ИЗМЕНЕНИЕ**: вносить правки максимально аккуратно
5. **ТЕСТИРОВАНИЕ**: проверить что виджет работает после изменений
6. **ПРОВЕРКА САЙТА**: убедиться что сайт и админка не сломались
7. **ДОКУМЕНТИРОВАНИЕ**: описать что исправлено (если логика изменена)

#### 🧪 ОБЯЗАТЕЛЬНОЕ ТЕСТИРОВАНИЕ ПОСЛЕ РАБОТЫ С ВИДЖЕТАМИ:
- Проверить работу фронтенда сайта (HTTP статус, загрузка страниц)
- Проверить работу админки (вход, основные функции)
- Проверить функционал самого виджета (все его возможности)
- Проверить логи на отсутствие новых ошибок
- Проверить связанные виджеты (которые могут зависеть от изменяемого)

#### ⚠️ КРИТИЧЕСКИЕ ПРЕДУПРЕЖДЕНИЯ:
- ВИДЖЕТЫ - это сердце функционала сайта, их поломка = поломка бизнеса
- Каждый виджет может влиять на другие части системы
- Всегда работать с виджетами как с критически важным кодом
- При малейшем сомнении - лучше НЕ трогать, чем сломать
- Если виджет работает - не чини то, что не сломано

## 🎛️ ПРАВИЛА РАБОТЫ С ПЛАВАЮЩИМИ ВИДЖЕТАМИ

### ✨ КЛЮЧЕВЫЕ ПРИНЦИПЫ ВИДЖЕТОВ:

1. **ПОСТОЯННАЯ ВИДИМОСТЬ**
   - Виджеты ВСЕГДА видны на экране, независимо от активной секции
   - Виджеты отображаются как плавающие элементы поверх основного контента
   - При переключении между сервисами виджеты НЕ исчезают

2. **НЕЗАВИСИМОСТЬ ОТ СЕРВИСОВ**
   - При открытии виджетов НЕ закрываются другие активные сервисы
   - Виджеты работают параллельно с любыми другими сервисами
   - Переключение на виджеты НЕ меняет активную секцию интерфейса

3. **ИНТЕРАКТИВНОСТЬ**
   - Виджеты можно перетаскивать по экрану
   - Виджеты сохраняют свое положение между сессиями
   - Виджеты имеют независимые z-index для правильного наложения

4. **ИНТЕГРАЦИЯ С ИНТЕРФЕЙСОМ**
   - Виджеты интегрированы в основной интерфейс без отдельной страницы
   - При клике на виджет в боковой панели выделяется только категория виджета
   - Виджеты не блокируют работу с другими компонентами

### 🔧 ТЕХНИЧЕСКАЯ РЕАЛИЗАЦИЯ:

- **WidgetsServiceTabs** - Основной компонент для отображения виджетов
- **TimeWidget** - Виджет времени с настройками формата
- **VoiceWidget** - Виджет голосового ввода с распознаванием речи
- **Позиционирование** - CSS переменные `--widget-x`, `--widget-y`, `--widget-z`
- **Темы** - Виджеты автоматически адаптируются к выбранной теме

### 🎨 СТИЛИЗАЦИЯ ВИДЖЕТОВ:

Виджеты используют систему CSS переменных для адаптации к темам:

```css
.widget-container {
  position: fixed;
  background: var(--card-bg);
  border: 1px solid var(--card-border);
  color: var(--text);
  /* ... */
}
```

## ⚠️ ЗАПРЕТЫ И АНТИПАТТЕРНЫ
- НЕ СОЗДАВАТЬ ОТЧЕТНЫЕ ФАЙЛЫ: не делать новые .md файлы с отчетами об исправлениях, просто писать в чате что исправлено
- НЕ СОЗДАВАТЬ DEBUG ФАЙЛЫ: не делать отдельные файлы для отладки, если затронута ключевая логика программы - обновлять документацию в основных файлах проекта (README, комментарии в коде)
- НЕ делать изменения без понимания корневой причины проблемы
- НЕ создавать множественные файлы для дебага
- НЕ игнорировать влияние изменений на другие части системы
- НЕ предлагать решения без предварительного исследования
- НЕ нарушать принципы SOLID и clean code
- НЕ смешивать разные типы ответственности в одном компоненте
- НЕ использовать hardcoded values вместо конфигурации
- НЕ пренебрегать тестированием edge cases и error scenarios
- НЕ ЗАСОРЯТЬ ПРОЕКТ ДОКУМЕНТАЦИЕЙ: не создавать множество новых .md файлов, ВМЕСТО ЭТОГО обновлять существующие файлы проекта только если изменена ключевая логика программы
- НЕ удаля файлы .cursorrules и Курсор проекты.code-workspace никогда.
- **НЕ ТРОГАТЬ ВИДЖЕТЫ** без понимания их полной архитектуры и зависимостей
- **НЕ ИЗМЕНЯТЬ ЛОГИКУ ВИДИМОСТИ ВИДЖЕТОВ** - они должны быть всегда видны
- **НЕ ПЕРЕКЛЮЧАТЬ СЕКЦИИ** при работе с виджетами
